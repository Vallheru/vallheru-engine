# Agent Instructions

- Keep all descriptions, documentation, and code comments in English.
- Follow repository conventions when updating the Rust workspace structure.
- When preparing pull requests for the ported workspace, always target the `develop` branch instead of `work`.
